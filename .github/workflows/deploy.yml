# 已弃用：站点已迁移到 Vercel（见 vercel.json / api/）。
# 保留文件以便回滚，但此 workflow 不再部署，防止旧前台（含明文凭据）被 CI 再次发布。
name: Deploy to GitHub Pages (DISABLED)

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy-disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy disabled
        run: echo "Deployment disabled — site has migrated to Vercel."